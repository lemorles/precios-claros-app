import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar navbar-dark fixed-top bg-dark">
      <h5 className="mx-auto lead text-light">App precios</h5>
    </nav>
  );
};

export default Navbar;
